spring:
  application:
    name: dummy-service

  cloud:
    consul:
      host: localhost
      port: 8500
      config:
        enabled: true
        format: yaml
        profile-separator: "-"
        fail-fast: true
